{
  "name": "auradigital",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker-entry.js && node --check src/worker.js && node --check src/security.js && node --check src/http.js && node --check src/notifications.js && node --check admin/admin.js",
    "test": "node --test test/*.test.mjs",
    "deploy:check": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "cf:types": "wrangler types worker-configuration.d.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260811.1",
    "wrangler": "file:tools/wrangler-safe"
  }
}
